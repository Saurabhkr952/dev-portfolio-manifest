apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-portfolio
  labels:
    app: dev-portfolio
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dev-portfolio
  template:
    metadata:
      labels:
        app: dev-portfolio
    spec:
      imagePullSecrets:
      - name: regsecret
      containers:
      - name: dev-portfolio
        image: 278445542791.dkr.ecr.ap-south-1.amazonaws.com/react-app:d50a656ca070537970659ee875eecb6b0bcf30cb
        imagePullPolicy: Always
        ports:
        - containerPort: 80
